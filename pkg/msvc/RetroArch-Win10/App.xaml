<Application
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RetroArch_Win10"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:Common="using:RetroArch_Win10.Common" mc:Ignorable="d"
    x:Class="RetroArch_Win10.App"
    RequestedTheme="Dark">

    <Application.Resources>
        <ResourceDictionary>
            <Common:BooleanVisibilityConverter x:Key="BooleanVisibilityConverter" Negate="False"/>
            <Common:BooleanVisibilityConverter x:Key="NegativeBooleanVisibilityConverter" Negate="True"/>
            <local:ViewModelLocator x:Key="ViewModelLocator"/>
            <Style x:Key="SystemListViewTemplate" TargetType="ListView">
                <Setter Property="IsTabStop" Value="False"/>
                <Setter Property="TabNavigation" Value="Once"/>
                <Setter Property="IsSwipeEnabled" Value="True"/>
                <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
                <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
                <Setter Property="ScrollViewer.HorizontalScrollMode" Value="Disabled"/>
                <Setter Property="ScrollViewer.IsHorizontalRailEnabled" Value="False"/>
                <Setter Property="ScrollViewer.VerticalScrollMode" Value="Enabled"/>
                <Setter Property="ScrollViewer.IsVerticalRailEnabled" Value="True"/>
                <Setter Property="ScrollViewer.ZoomMode" Value="Disabled"/>
                <Setter Property="ScrollViewer.IsDeferredScrollingEnabled" Value="False"/>
                <Setter Property="ScrollViewer.BringIntoViewOnFocusChange" Value="True"/>
                <Setter Property="ItemContainerTransitions">
                    <Setter.Value>
                        <TransitionCollection>
                            <AddDeleteThemeTransition/>
                            <ContentThemeTransition/>
                            <ReorderThemeTransition/>
                            <EntranceThemeTransition IsStaggeringEnabled="False"/>
                        </TransitionCollection>
                    </Setter.Value>
                </Setter>
                <Setter Property="ItemsPanel">
                    <Setter.Value>
                        <ItemsPanelTemplate>
                            <ItemsStackPanel Orientation="Vertical"/>
                        </ItemsPanelTemplate>
                    </Setter.Value>
                </Setter>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ListView">
                            <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                                <ScrollViewer x:Name="ScrollViewer" AutomationProperties.AccessibilityView="Raw" BringIntoViewOnFocusChange="{TemplateBinding ScrollViewer.BringIntoViewOnFocusChange}" HorizontalScrollMode="{TemplateBinding ScrollViewer.HorizontalScrollMode}" HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}" IsHorizontalRailEnabled="{TemplateBinding ScrollViewer.IsHorizontalRailEnabled}" IsHorizontalScrollChainingEnabled="{TemplateBinding ScrollViewer.IsHorizontalScrollChainingEnabled}" IsVerticalScrollChainingEnabled="{TemplateBinding ScrollViewer.IsVerticalScrollChainingEnabled}" IsVerticalRailEnabled="{TemplateBinding ScrollViewer.IsVerticalRailEnabled}" IsDeferredScrollingEnabled="{TemplateBinding ScrollViewer.IsDeferredScrollingEnabled}" TabNavigation="{TemplateBinding TabNavigation}" VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}" VerticalScrollMode="{TemplateBinding ScrollViewer.VerticalScrollMode}" ZoomMode="{TemplateBinding ScrollViewer.ZoomMode}">
                                    <ItemsPresenter FooterTransitions="{TemplateBinding FooterTransitions}" FooterTemplate="{TemplateBinding FooterTemplate}" Footer="{TemplateBinding Footer}" HeaderTemplate="{TemplateBinding HeaderTemplate}" Header="{TemplateBinding Header}" HeaderTransitions="{TemplateBinding HeaderTransitions}" Padding="{TemplateBinding Padding}"/>
                                </ScrollViewer>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            </Style>
            <DataTemplate x:Key="SystemListDataTemplate">
                <Grid Height="100" VerticalAlignment="Top">
                    <Grid.Background>
                        <ImageBrush Stretch="UniformToFill"/>
                    </Grid.Background>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Viewbox>
                        <Image x:Name="image" Height="100" Width="100" Source="{Binding Icon}"/>
                    </Viewbox>
                    <TextBlock x:Name="textBlock" TextWrapping="Wrap" Text="{Binding Name}" d:LayoutOverrides="Width" Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="8,0,0,0"/>
                </Grid>
            </DataTemplate>
            <Style x:Key="ListViewContainerStyle" TargetType="ListViewItem">
                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            </Style>
            <DataTemplate x:Key="GameListEntryTemplate">
                <Grid Padding="0" Margin="0,0,0,4" d:DesignWidth="487.905" Width="300">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Grid x:Name="Default" Grid.Column="2" Margin="0" Visibility="{Binding Importing, Converter={StaticResource NegativeBooleanVisibilityConverter}}">
                        <TextBlock x:Name="textBlock" TextWrapping="Wrap" Text="{Binding Title, FallbackValue=Game Title}" d:LayoutOverrides="TopMargin, BottomMargin" VerticalAlignment="Center"/>
                    </Grid>
                    <StackPanel x:Name="Importing" Grid.Column="2" Orientation="Vertical" d:LayoutOverrides="Height" Visibility="{Binding Importing, Converter={StaticResource BooleanVisibilityConverter}, Mode=OneWay}" VerticalAlignment="Center">
                        <TextBlock x:Name="textBlock_Copy" TextWrapping="Wrap" Text="{Binding Title, FallbackValue=Game Title}" d:LayoutOverrides="TopMargin, BottomMargin"/>
                        <ProgressBar VerticalAlignment="Stretch" Height="10" Maximum="1" Value="{Binding Progress}"/>
                    </StackPanel>
                    <Image x:Name="image" Source="{Binding BoxArt, FallbackValue=ms-appx:///media/assets/xmb/flatui/png/Sega - Mega Drive - Genesis-content.png}" d:LayoutOverrides="LeftMargin, RightMargin, TopMargin, BottomMargin" Width="100" Height="100"/>
                </Grid>
            </DataTemplate>
            <DataTemplate x:Key="GameListEntryPendingTemplate">
                <Grid Padding="0" Margin="0,0,0,4" d:DesignWidth="475" d:DesignHeight="100">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <StackPanel x:Name="stackPanel" Grid.Column="2" Orientation="Vertical" d:LayoutOverrides="Height" VerticalAlignment="Center">
                        <TextBlock x:Name="textBlock_Copy" TextWrapping="Wrap" Text="{Binding Title, FallbackValue=Game Title}" VerticalAlignment="Center" d:LayoutOverrides="TopMargin, BottomMargin"/>
                        <ProgressBar VerticalAlignment="Stretch" Height="10" Maximum="1" IsIndeterminate="True"/>
                        <TextBlock x:Name="textBlock1" TextWrapping="Wrap" Text="Loading" TextAlignment="Center" FontSize="10.667" Foreground="#7FFFFFFF"/>
                    </StackPanel>
                    <Image x:Name="image" Source="{Binding BoxArt, FallbackValue=ms-appx:///media/assets/xmb/flatui/png/Sega - Mega Drive - Genesis-content.png}" d:LayoutOverrides="LeftMargin, RightMargin, TopMargin, BottomMargin" Width="100" Height="100"/>
                </Grid>
            </DataTemplate>
            <Thickness x:Key="ListHeaderMargin">8,0,0,8</Thickness>
        </ResourceDictionary>
    </Application.Resources>

</Application>
