<Page
    x:Class="RetroArch_Win10.content"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RetroArch_Win10"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" DataContext="{Binding ContentVM, Mode=OneWay, Source={StaticResource ViewModelLocator}}">
    <Page.BottomAppBar>
        <CommandBar HorizontalContentAlignment="Left">
            <AppBarButton Label="appbarbutton" Icon="Add" Click="Add_Clicked"/>
        </CommandBar>
    </Page.BottomAppBar>
    <Grid>
        <Grid.Background>
            <ImageBrush Stretch="UniformToFill" ImageSource="{Binding CoresVM.SelectedItem.Background, Source={StaticResource ViewModelLocator}}"/>
        </Grid.Background>
        <ListView x:Name="listView" ItemsSource="{Binding Games}" ItemTemplate="{StaticResource GameListEntryTemplate}" SelectionMode="None" ItemClick="ItemClick" IsItemClickEnabled="True"/>
    </Grid>
</Page>
