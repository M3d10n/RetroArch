<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RetroArch_Win10"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Interactivity="using:Microsoft.Xaml.Interactivity" xmlns:Core="using:Microsoft.Xaml.Interactions.Core"
    x:Class="RetroArch_Win10.cores"
    mc:Ignorable="d" RequestedTheme="Dark" DataContext="{Binding CoresVM, Source={StaticResource ViewModelLocator}}" NavigationCacheMode="Enabled">
    <ListView x:Name="listView" ItemsSource="{Binding Systems}" Style="{StaticResource SystemListViewTemplate}" ItemTemplate="{StaticResource SystemListDataTemplate}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}" IsTextScaleFactorEnabled="False" HorizontalContentAlignment="Stretch" ItemContainerStyle="{StaticResource ListViewContainerStyle}">
        <ListView.Header>
            <Border Padding="8">
                <TextBlock x:Name="textBlock" TextWrapping="Wrap" Text="Systems" FontSize="20" VerticalAlignment="Top"/>
            </Border>
        </ListView.Header>
    </ListView>
</Page>
