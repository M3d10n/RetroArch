<Page
    x:Class="RetroArch_Win10.cores"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RetroArch_Win10"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" RequestedTheme="Dark">
    <Page.Resources>

    </Page.Resources>
    <Page.DataContext>
        <Binding Path="CoresVM" Source="{StaticResource ViewModelLocator}"/>
    </Page.DataContext>
    <ListView x:Name="listView" ItemsSource="{Binding Systems}" Style="{StaticResource SystemListViewTemplate}" ItemTemplate="{StaticResource SystemListDataTemplate}" SelectedItem="{Binding SelectedItem}" IsTextScaleFactorEnabled="False" HorizontalContentAlignment="Stretch" ItemContainerStyle="{StaticResource ListViewContainerStyle}">
        <ListView.Header>
            <TextBlock x:Name="textBlock" TextWrapping="Wrap" Text="Systems"/>
        </ListView.Header>
    </ListView>
</Page>
